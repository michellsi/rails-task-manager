<h2>These are your tasks <i class="fa-solid fa-square-check"></i>✅</h2>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
      <%= link_to 'Delete',
                  task_path(task),
                  method: :delete,
                  data: { confirm: 'Are you sure you want to delete this taks?' } %>
    </li>
  <% end %>
</ul>
